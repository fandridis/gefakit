export { evaluateCondition, evaluateRule, evaluateFlag } from './utils';
export type { RuleCondition, RuleGroup, Rule, EvaluationContext, FeatureFlag, } from '@gefakit/shared';
export { isRuleGroup, isRuleCondition } from '@gefakit/shared';
